# GoreeCloud Mesh Center Static Website

Canonical static-site source now lives in `GoreeCloud/goreecloud-static-websites` under `sites/mesh/`.

This `website/` subtree and its repository-local public-site build/deployment helpers are retained only as transitional deployment/history material until Cloudflare Pages is cut over to the central repository and the exact production revision is verified. New authoritative Mesh Center static-site changes belong in the central repository.

GoreeCloud Mesh runtime code, APIs, evidence-plane contracts, coordination authority, and application/service implementation remain authoritative in this repository; only the public static website source is centralized.
